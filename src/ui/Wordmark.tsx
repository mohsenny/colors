import type { ReactElement } from 'react'

/** A museum label, not a logo: it names the object and then gets out of the way. */
export function Wordmark(): ReactElement {
  return <div className="lb-wordmark">Lightbox</div>
}
